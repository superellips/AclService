apiVersion: apps/v1
kind: Deployment
metadata:
  name: "aclservice-deployment"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "aclservice-app"
  template:
    metadata:
      labels:
        app: "aclservice-app"
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: aclservice-container
        image: ghcr.io/superellips/aclservice:fd1d37097c66da95203bc6de7d15e2bab470f50a
        resources:
          limits:
            cpu: "10"
            memory: "100Mi"
          requests:
            cpu: "10 "
            memory: "50Mi"
        ports:
        - containerPort: 8080
        envFrom: 
        - configMapRef:
            name: aclservice-config